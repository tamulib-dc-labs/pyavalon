# Copy to .env.local (gitignored) and fill in.
# Read automatically from the directory you run pyavalon in.
# Anything already set in your shell environment takes precedence.
AVALON_PRE=your-pre-instance-api-key
AVALON_PROD=your-prod-instance-api-key
